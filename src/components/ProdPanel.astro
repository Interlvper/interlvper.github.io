---
import { PROD_DELIVERABLES, PROD_SERVICES } from '../data/production';
import { REVIEWS } from '../data/reviews';
import ReviewCard from './ReviewCard.astro';
---

<section id="panel-prod" class="panel">
    <div class="panel-header">
        <h2 class="panel-title prod-title">Audio Production</h2>
    </div>

    <div id="prod-ghostwriting" class="data-block prod-glow" style="margin-bottom: 60px;">
        <h3>The Complete Package</h3>
        <div class="prod-services-grid">
            <div style="display: flex; flex-direction: column;">
                <p>
                    With over seven years of professional experience in modern metal, I specialize in providing complete
                    songwriting, mixing, and mastering services. From the initial idea to the final master, I work with you to ensure
                    the final product is something that we are both proud of.
                </p>
                <p>This
                    is an all-in-one, premium service designed for artists, vocalists, and content creators who
                    need industry quality music. Upon delivery, you retain <strong>100% Commercial
                        Rights</strong> and complete ownership of the track and its copyrights.</p>
                <div style="margin-top: auto;">
                    <a href="#panel-contact" class="btn-action" style="align-self: flex-start;">Inquire Here</a>
                </div>
            </div>
            <div class="nested-block" style="align-self: start;">
                <h4>Exact Deliverables</h4>
                <ul class="deliverables-list">
                    {PROD_DELIVERABLES.map(d => (
                        <li set:html={d} />
                    ))}
                </ul>
            </div>
        </div>
    </div>

    <div id="prod-engineering" class="data-grid" style="margin-bottom: 60px;">
        {PROD_SERVICES.map(svc => (
            <div class="data-block prod-glow">
                <h3>{svc.title}</h3>
                <p>{svc.desc}</p>
                <a href="#panel-contact" class="btn-action">{svc.btnText}</a>
            </div>
        ))}
    </div>

    <div id="prod-fiverr" class="data-block prod-glow" style="margin-bottom: 60px;">
        <h3>Marketplace Booking</h3>
        <p>While I prefer establishing direct, organic relationships with my clients, I also maintain an active,
            Top Rated profile on Fiverr for clients who prefer to utilize a third-party platform. All reviews
            displayed
            below are verified through this platform or other previous work.</p>
        <a href="https://www.fiverr.com/users/tylerjmyles" target="_blank" rel="noopener noreferrer" class="btn-action">View Fiverr Profile</a>
    </div>

    <div id="prod-feedback" class="data-block prod-glow">
        <h3>Client Feedback</h3>
        <p>Organic reviews from artists, bands, and creators I've engineered for.</p>
        <div class="review-grid" style="margin-bottom: 0;">
            {REVIEWS.map(review => (
                <ReviewCard review={review} />
            ))}
        </div>
    </div>
</section>
